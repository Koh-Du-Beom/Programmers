// 프로그래머스 lv1 둘만의 암호

function solution(s, skip, index) {
  s.forEach((item) => {
    let indexedItem = item.charCodeAt(0);
    for (let i=0; i<index; i++){
      if (s === skip[i]){

      }
    }
    // for문을 무사히 벗어났다면, 즉 skip안에 있는 문자열이 아니라면
    
  })
  var answer = '';
  return answer;
}